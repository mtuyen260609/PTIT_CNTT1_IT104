const username: string = "Nam";
const age: number = 18;
const job: string = "Develop";

const printUser = (username:string, age:number, job:string) => {
    console.log(`Tên: ${username}`);
    console.log(`Tuổi: ${age}`);
    console.log(`Nghề ngiệp: ${job}`);
    
}
printUser(username, age, job);